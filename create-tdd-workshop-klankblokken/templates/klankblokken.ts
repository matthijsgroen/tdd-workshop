/**
 * Because the type signature does not yet match with the test,
 * this issue: https://github.com/kulshekhar/ts-jest/issues/943
 * can require you to restart the test suite.
 */
export const classifyWord = () => {};
